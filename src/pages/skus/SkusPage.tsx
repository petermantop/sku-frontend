import React from "react";
import Layout from "../../layout/Layout";
import SkusList from "../../sections/skus/list";

const SkusPage = () => {
  return (
    <Layout>
      <SkusList />
    </Layout>
  );
};

export default SkusPage;
